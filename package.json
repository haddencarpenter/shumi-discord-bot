{
  "name": "shumi-discord-bot",
  "version": "1.0.0",
  "description": "Shumi Trading Competition Discord Bot - Phase 1 Foundation",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "prestart": "node -e \"const {execSync}=require('node:child_process'); const fs=require('node:fs'); let v='dev'; try{v=execSync('git rev-parse --short HEAD').toString().trim(); console.log('✅ Git hash:', v)}catch(e){console.log('❌ Git failed:', e.message)} try{fs.writeFileSync('src/version.generated.json', JSON.stringify({git:v,builtAt:new Date().toISOString()})); console.log('✅ Version file written:', v)}catch(e){console.log('❌ File write failed:', e.message)}\"",
    "start": "node src/index.js",
    "sentiment": "node src/sentiment.js",
    "test:regression": "node scripts/test-regression.js",
    "test:canonical": "node test-canonical-regression.js",
    "predeploy": "npm run test:regression && npm run test:canonical",
    "migrate:up": "DATABASE_URL=\"${MIGRATOR_DATABASE_URL:-$DATABASE_URL}\" node-pg-migrate up --migrations-dir migrations --no-check-order",
    "migrate:down": "DATABASE_URL=\"${MIGRATOR_DATABASE_URL:-$DATABASE_URL}\" node-pg-migrate down --migrations-dir migrations",
    "migrate:create": "node-pg-migrate create --migrations-dir migrations",
    "db:backup": "mkdir -p backups && pg_dump \"$DATABASE_URL\" -Fc -f backups/shumi_$(date +%F_%H%M%S).dump && echo 'Backup created'",
    "db:sanity": "psql \"$DATABASE_URL\" -c \"select count(*) from pg_tables where schemaname='public';\"",
    "prepare": "husky || true",
    "test:ws:sd": "node src/tests/ws-only-sd.js"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "discord.js": "^14.21.0",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "node-cron": "^4.2.1",
    "node-pg-migrate": "^8.0.3",
    "pg": "^8.16.3",
    "sqlite3": "^5.1.6",
    "ws": "^8.18.3"
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "keywords": [
    "discord",
    "bot",
    "trading",
    "competition",
    "crypto"
  ],
  "author": "Shumi Team",
  "license": "MIT"
}
